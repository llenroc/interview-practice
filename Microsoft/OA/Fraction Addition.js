/**
 * If you notice that if a pair of fraction can add to 1, they must share same denominator.
 *  You just need to save all the possible numerator of one denominator into a map.
 * Then it become a problem of two sum.
 */